name: NBA Daily Scores

on:
  schedule:
    - cron: "15 6 * * *"
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install requests

      - name: Fetch NBA scores
        run: python scripts/fetch_nba.py

      - name: Upload to Dropbox (debug)
        env:
          DROPBOX_TOKEN: ${{ secrets.DROPBOX_TOKEN }}
        run: |
          echo "Uploading nba.json to Dropbox…"
          curl -v -X POST https://content.dropboxapi.com/2/files/upload \
            --header "Authorization: Bearer $DROPBOX_TOKEN" \
            --header "Dropbox-API-Arg: {\"path\": \"/nba/nba.json\", \"mode\": \"overwrite\", \"autorename\": false}" \
            --header "Content-Type: application/octet-stream" \
            --data-binary @nba.json

